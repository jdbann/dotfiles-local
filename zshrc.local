[ -f ~/fzf.zsh ] && source ~/.fzf.zsh

eval "$(/opt/homebrew/bin/brew shellenv)"

export PATH="$HOME/.bin:$PATH"
source /opt/homebrew/opt/asdf/asdf.sh

# Start the starhip shell
eval "$(starship init zsh)"
